{
  "name": "openInNewTabIntent",
  "description": "Tests if the current event seems like an intent to open a new tab. Useful for client-side navigation handling.",
  "signature": "({\n  button,\n  ctrlKey,\n  metaKey,\n  shiftKey\n}: {\n  button?: number;\n  ctrlKey?: boolean;\n  metaKey?: boolean;\n  shiftKey?: boolean;\n}) => boolean",
  "examples": [
    {
      "language": "javascript",
      "content": "openInNewTabIntent({ ctrlKey: true });\n// ⇒ true"
    }
  ],
  "questions": [
    "How to check if user wants to open a new tab using history API?"
  ]
}
