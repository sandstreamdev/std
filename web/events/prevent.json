{
  "name": "prevent",
  "description": "Prevents the default handler of the given event.",
  "signature": "(event: { preventDefault: () => void }) => boolean",
  "examples": [
    {
      "language": "javascript",
      "content": "const event = {\n  preventDefault: () => console.log(\"preventDefault\"),\n  stopPropagation: () => console.log(\"stopPropagation\")\n};\n\nprevent(event);"
    }
  ],
  "questions": ["How to prevent default event handler?"]
}
