{
  "name": "cancel",
  "description": "Stops propagation and prevents the default handler of the given event.",
  "signature": "(event: {\n  preventDefault: () => void;\n  stopPropagation: () => void;\n}) => boolean",
  "examples": [
    {
      "language": "javascript",
      "content": "const event = {\npreventDefault: () => console.log(\"preventDefault\"),\nstopPropagation: () => console.log(\"stopPropagation\")\n};\n\ncancel(event);"
    }
  ],
  "questions": ["How to stop propagation and prevent default at once?"]
}
