{
  "name": "partition",
  "description": "Partitions the given array to the ones that pass the given predicate function and the ones that do not. By [convention of the Haskell's Data.Either](http://hackage.haskell.org/package/base-4.12.0.0/docs/Data-Either.html), values that pass the predicate are placed at right.",
  "signature": "(predicate: (x: any) => boolean) => (xs: any[]) => any",
  "examples": [
    {
      "language": "javascript",
      "content": "partition(x => x % 2 === 1)([1, 2, 3, 4, 5]);\n// ⇒ [[2, 4], [1, 3, 5]])"
    }
  ],
  "questions": [
    "How to partition an array based on a condition?",
    "How to divide an array by a filter function?"
  ]
}
