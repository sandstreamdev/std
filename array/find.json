{
  "name": "find",
  "description": "Finds an element by a predicate function within the given array, otherwise, it returns the given fallback value or undefined when fallback is not present.",
  "signature": "<T>(predicate: (value: T, index: number, context: T[]) => boolean, fallback?: T) => (xs: T[]) => T",
  "examples": [
    {
      "language": "javascript",
      "content": "find(x => x > 2)([1, 2, 3, 5, 7]);\n// ⇒ 3"
    },
    {
      "language": "javascript",
      "content": "find(x => x > 2)([1, 2, -3, -5, -7]);\n// ⇒ undefined"
    }
  ],
  "questions": ["How to find an element of an array by a given predicate?"]
}
