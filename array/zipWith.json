{
  "name": "zipWith",
  "description": "Zips given arrays together with the given function.",
  "signature": "(\n  f?: (x: any, y: any) => any[]\n) => (xs: any[], ys: any[]) => any[][]",
  "examples": [
    {
      "language": "javascript",
      "content": "zipWith((x, y) => x * x + y)([1, 2, 3], [4, 5, 6]);\n// ⇒ [5, 9, 15]"
    }
  ],
  "questions": ["How to zip two arrays with a given function?"]
}
