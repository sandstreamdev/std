{
  "name": "mapValues",
  "description": "Maps and returns an array of transformed object values.",
  "signature": "(\n  f: (value: any, key: string, context: object) => any\n) => (xs: object) => any[]",
  "examples": [
    {
      "language": "javascript",
      "content": "mapValues(x => x ** 2)({ a: 1, b: 2, c: 3 });\n// ⇒ [1, 4, 9]"
    }
  ],
  "questions": ["How to map object values?"]
}
