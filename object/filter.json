{
  "name": "filter",
  "description": "Filters the given object with the given predicate.",
  "signature": "(\n  f: (value: any, key: string, context: object) => boolean\n) => (xs: object) => object",
  "examples": [
    {
      "language": "javascript",
      "content": "filter(x => x % 2 !== 0)(({ a: 1, b: 2, c: 3 }); // ⇒ { a: 1, c: 3 }"
    }
  ],
  "questions": ["How to filter an object?"]
}
