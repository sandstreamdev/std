{
  "name": "filter",
  "description": "Filters the given object with the given predicate.",
  "signature": "<T>(f: (value: T, key: string, context: object) => boolean) => (xs: GenericObject<T>) => GenericObject<T>",
  "examples": [
    {
      "language": "javascript",
      "content": "filter(x => x % 2 !== 0)({ a: 1, b: 2, c: 3 });\n// ⇒ { a: 1, c: 3 }"
    }
  ],
  "questions": ["How to filter an object?"]
}
