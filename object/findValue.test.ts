/* eslint-env jest */
// @ts-ignore ambiguous import
import findValue from "./findValue.ts";

describe("findValue", () => {
  it.skip("TODO", () => {
    expect(findValue()).toBeDefined();
  });
});
