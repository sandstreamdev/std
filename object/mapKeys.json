{
  "name": "mapKeys",
  "description": "Transforms the object keys with the given function.",
  "signature": "<T>(f: (value: T, key: string, context: object) => string) => (xs: GenericObject<T>) => GenericObject<T>",
  "examples": [
    {
      "language": "javascript",
      "content": "mapKeys((_, key) => key.toUpperCase())({ a: 1, b: 2, c: 3 });\n// ⇒ { A: 1, B: 2, C: 3 }"
    }
  ],
  "questions": ["How to map object keys?", "How to transform object keys?"]
}
