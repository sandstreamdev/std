{
  "name": "flatMapValues",
  "description": "Flat maps the values of the given object.",
  "signature": "(\n  f: (value: any, key: string, context: object) => boolean\n) => (xs: object) => any[]",
  "examples": [
    {
      "language": "javascript",
      "content": "flatMapValues(); // ⇒ TODO"
    }
  ],
  "questions": ["TODO: List questions that may this function answers."]
}
