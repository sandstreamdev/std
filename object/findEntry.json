{
  "name": "findEntry",
  "description": "Searches the given object by the given predicate and returns the found entry or undefined.",
  "signature": "<T>(predicate: (value: T, key: string, context: GenericObject<T>) => boolean) => (xs: GenericObject<T>) => [string, T] | undefined",
  "examples": [
    {
      "language": "javascript",
      "content": "findEntry(({ x }) => x % 2 === 0)({ a: { x: 1 }, b: { x: 2 }, c: { x: 3 } });\n// ⇒ [\"b\", { x: 2 }]"
    }
  ],
  "questions": ["How to find an entry of an object by a predicate function?"]
}
