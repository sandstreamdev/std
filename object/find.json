{
  "name": "find",
  "description": "Searches the given object by the given predicate and returns the found value or undefined.",
  "signature": "<T>(predicate: (value: T, key: string, context: object) => boolean) => (xs: GenericObject<T>) => T | undefined",
  "examples": [
    {
      "language": "javascript",
      "content": "find(({ x }) => x % 2 === 0)({ a: { x: 1 }, b: { x: 2 }, c: { x: 3 } });\n// ⇒ { x: 2 }"
    }
  ],
  "questions": ["How to find the value of an object by a predicate function?"]
}
