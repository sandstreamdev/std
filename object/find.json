{
  "name": "find",
  "description": "Searches the given object by the given predicate and returns the found value or undefined.",
  "signature": "(\n  predicate: (value: any, key: string, context: object) => boolean\n) => (xs: object) => any",
  "examples": [
    {
      "language": "javascript",
      "content": "find(({ x }) => x % 2 === 0)(({ a: { x: 1 }, b: { x: 2 }, c: { x: 3 } }); // ⇒ { x: 2 }"
    }
  ],
  "questions": ["How to find a value of an object by a function?"]
}
