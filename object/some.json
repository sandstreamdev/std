{
  "name": "some",
  "description": "Test if any element passes the given predicate.",
  "signature": "(\n  f: (value: any, key: string, context: object) => boolean\n) => (xs: object) => boolean",
  "examples": [
    {
      "language": "javascript",
      "content": "some(x => x >= 4)({ x: 5, y: 3, z: 0 });\n// ⇒ true"
    },
    {
      "language": "javascript",
      "content": "some(x => x < 0)({ x: 5, y: 3, z: 0 });\n// ⇒ false"
    }
  ],
  "questions": [
    "How to check if any entry in an object passes a given predicate?"
  ]
}
