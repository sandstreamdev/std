{
  "name": "findKey",
  "description": "Searches the given object by the given predicate and returns the found key or undefined.",
  "signature": "(\n  predicate: (value: any, key: string, context: object) => boolean\n) => (xs: object) => any",
  "examples": [
    {
      "language": "javascript",
      "content": "findKey(({ x }) => x % 2 === 0)({ a: { x: 1 }, b: { x: 2 }, c: { x: 3 } });\n// ⇒ \"b\""
    }
  ],
  "questions": ["How to find a key of an object by a predicate function?"]
}
