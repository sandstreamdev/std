import assert from "./assert";
import diff from "./diff";

export { assert, diff };
declare const _default: {
  assert: (condition: any, callbackOrMessage: any) => void;
  diff: (obj1: any, obj2: any) => any;
};

export default _default;
