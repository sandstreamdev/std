{
  "name": "debounce",
  "description": "Makes the function run after the given period of not being called. Useful to delay input submission for autocomplete etc.",
  "signature": "(\n  f: (...args: any[]) => any,\n  wait: number\n) => (...args: any[]) => void",
  "examples": [
    {
      "language": "javascript",
      "content": "debounce(); // ⇒ TODO"
    }
  ],
  "questions": ["TODO: List questions that may this function answers."]
}
