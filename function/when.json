{
  "name": "when",
  "description": "Runs the given function only when the condition is met.",
  "signature": "(\n  predicate: (...xs: any[]) => boolean\n) => (action: (...xs: any[]) => any) => (...args: any[]) => any",
  "examples": [
    {
      "language": "javascript",
      "content": "when(x => x > 0)(x => console.log(x))(5);\nwhen(x => x > 0)(x => console.log(x))(-3);"
    }
  ],
  "questions": ["How to run a function only when a condition is satisfied?"]
}
