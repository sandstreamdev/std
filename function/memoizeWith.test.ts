/* eslint-env jest */
// @ts-ignore ambiguous import
import memoizeWith from "./memoizeWith.ts";

describe("memoizeWith", () => {
  it("TODO", () => {
    expect(memoizeWith()).toBeDefined();
  });
});
