{
  "name": "escape",
  "description": "Escapes regex string into proper regex.",
  "signature": "(string: string) => string",
  "examples": [
    {
      "language": "javascript",
      "content": "escape(\"te.t\").test(\"text\");\n// ⇒ false"
    }
  ],
  "questions": ["How to properly escape a regex string?"]
}
