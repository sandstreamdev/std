{
  "name": "escape",
  "description": "Escapes regex string into proper regex.",
  "signature": "(string: string) => string",
  "examples": [
    {
      "language": "javascript",
      "content": "escape(); // ⇒ TODO"
    }
  ],
  "questions": ["TODO: List questions that may this function answers."]
}
