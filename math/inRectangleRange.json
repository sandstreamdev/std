{
  "name": "inRectangleRange",
  "description": "Checks if the given value is in the rectangular range of [0, width] and [0, height]",
  "signature": "(\n  width: number,\n  height: number\n) => (x: number, y: number) => boolean",
  "examples": [
    {
      "language": "javascript",
      "content": "inRectangleRange(50, 100)(25, 50); // ⇒ true"
    },
    {
      "language": "javascript",
      "content": "inRectangleRange(50, 100)(-25, 50); // ⇒ false"
    }
  ],
  "questions": [
    "How to check if a point is inside a rectangle defined by width and height?"
  ]
}
