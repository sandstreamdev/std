/* eslint-env jest */
// @ts-ignore ambiguous import
import startsWith from "./startsWith.ts";

describe("startsWith", () => {
  it.skip("TODO", () => {
    expect(startsWith()).toBeDefined();
  });
});
