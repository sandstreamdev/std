{
  "name": "split",
  "description": "Splits the given range in subranges by excluding the given used ranged.",
  "signature": "(\n  used: [number, number][],\n  sourceRange?: number[]\n) => (range: [number, number]) => [number, number][]",
  "examples": [
    {
      "language": "javascript",
      "content": "split(); // ⇒ TODO"
    }
  ],
  "questions": ["TODO: List questions that may this function answers."]
}
