/* eslint-env jest */
// @ts-ignore ambiguous import
import daysInYear from "./daysInYear.ts";

describe("daysInYear", () => {
  it.skip("TODO", () => {
    expect(daysInYear()).toBeDefined();
  });
});
