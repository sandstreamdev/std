/* eslint-env jest */
// @ts-ignore ambiguous import
import fromMinutes from "./fromMinutes.ts";

describe("fromMinutes", () => {
  it("TODO", () => {
    expect(fromMinutes()).toBeDefined();
  });
});
