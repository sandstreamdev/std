/* eslint-env jest */
// @ts-ignore ambiguous import
import toMinutes from "./toMinutes.ts";

describe("toMinutes", () => {
  it.skip("TODO", () => {
    expect(toMinutes()).toBeDefined();
  });
});
