/* eslint-env jest */
// @ts-ignore ambiguous import
import toSeconds from "./toSeconds.ts";

describe("toSeconds", () => {
  it.skip("TODO", () => {
    expect(toSeconds()).toBeDefined();
  });
});
