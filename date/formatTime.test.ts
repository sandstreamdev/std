/* eslint-env jest */
// @ts-ignore ambiguous import
import formatTime from "./formatTime.ts";

describe("formatTime", () => {
  it.skip("TODO", () => {
    expect(formatTime()).toBeDefined();
  });
});
