/* eslint-env jest */
// @ts-ignore ambiguous import
import formatDate from "./formatDate.ts";

describe("formatDate", () => {
  it.skip("TODO", () => {
    expect(formatDate()).toBeDefined();
  });
});
