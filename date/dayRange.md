# dayRange

## Type signature

```typescript
<!-- prettier-ignore-start -->
({
  iso,
  local,
  now,
  timezoneOffset
}: {
  iso?: boolean;
  local?: boolean;
  now?: Date;
  timezoneOffset?: number;
}) => (date: any) => any[]
<!-- prettier-ignore-end -->
```
