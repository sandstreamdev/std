/* eslint-env jest */
// @ts-ignore ambiguous import
import monthNames from "./monthNames.ts";

describe("monthNames", () => {
  it.skip("TODO", () => {
    expect(monthNames()).toBeDefined();
  });
});
